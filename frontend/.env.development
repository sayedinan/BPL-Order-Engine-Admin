# Default dev env. Mock mode is on by default until the v0.3 backend
# lands. Set VITE_USE_MOCK=false (or rename this file) to call the
# real backend at VITE_API_BASE_URL.
VITE_USE_MOCK=true
VITE_API_BASE_URL=http://localhost:8080
